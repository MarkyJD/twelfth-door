import {
  collection,
  query,
  where,
  getDocs,
  getDoc,
  limit,
  arrayRemove,
  doc,
  arrayUnion,
  updateDoc,
} from 'firebase/firestore';
import { db } from '../lib/firebase';

export default function getUserByUserId(uid) {
  return null;
}
